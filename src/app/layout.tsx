import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DnP Technologies - Empowering Digital Transformation",
  description: "DnP Technologies Private Limited - IT Services & Consulting specializing in Telecom Solutions, Cloud Infrastructure, DevOps, and Digital Transformation.",
  keywords: "telecom solutions, cloud services, DevOps, IT consulting, software development, AAA systems, RADIUS, 5G solutions, Navi Mumbai",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <Header />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
